# 公开发布门禁、GitHub 与 Marketplace 状态（2026-08-20）

## 2026-08-20 公开发布准备更新

- 已确认 GitHub 登录账号为 `yzy-gm`；目标仓库 `yzy-gm/yuanmeng-ai-dev-assistant` 已创建为 `PUBLIC`，公开快照已推送到 `main`。
- `package.json` 已写入真实 `repository`、`homepage`、`bugs` URL，`package:vsix` 不再使用 `--allow-missing-repository`。
- 同一路径候选 VSIX 已重新生成：18 文件、约 221.31 KB，SHA-256 `AE68AFC16365CD1C3F06727F823579476F7E9A0C483EA0620EB5583581BEF060`；VSIX 清单检查零发现，生产依赖审计为 0 个漏洞。
- 本节记录 GitHub 公开源码交付；不代表 Marketplace Publisher 已创建或扩展已上线。

## 当前门禁

| 门 | 状态 | 证据 |
| --- | --- | --- |
| TypeScript strict、单元、集成、Extension Host | PASS | `docs/acceptance/2026-08-19-automated.md` |
| CLI launcher 中文/空格路径、绑定和无 PATH | PASS | Task 17 集成与 Extension Host 测试 |
| UI Schema 真实校准 | PASS（仅导出观察） | M1 correction evidence |
| 100,000 节点 RSS `<512 MiB` | UNVERIFIED | 六次运行 491–547 MB，其中一次硬门失败；时间指标通过 |
| VSCode 1.70.3 | UNVERIFIED | 下载入口不可用 |
| VSIX 打包与 Clean Profile 生命周期 | PASS（VSCode 1.134.0 隔离 Profile） | automated acceptance |
| 官方属性推送/脚本合成/多人 | UNVERIFIED | 需要真实编辑器操作和观察 |
| GitHub 公开仓库 | PASS | `yzy-gm/yuanmeng-ai-dev-assistant` 为 `PUBLIC`，远端 `main` 已核对 |
| 计划的 Marketplace Publisher `bujianxingguang` | NOT STARTED | 官方页面尚未创建/确认；ID 创建后才最终确定 |
| Marketplace 发布与搜索安装 | NOT STARTED | 必须使用最终审计 VSIX，并在发布后用干净 Profile 验证 |

最新本机候选：`npm run verify` 通过（23 个单元测试文件/145 项、11 个集成测试文件/67 项、稳定版 Extension Host 两轮）；最小 VSIX 为 18 文件、221.17 KB，内容检查零发现，并再次通过 VSCode 1.134.0 隔离 Profile 的安装、激活、升级、迁移、禁用/重启、卸载和旧 launcher 安全失败生命周期。`package:release` 生成的同路径文件 SHA-256 为 `19EB2CAD878A4AA287AFD51D509B6D633BA40B88A2735F329ABFAC68E1F487E1`。包内清单已确认作者、description、256×256 PNG 与 LICENSE，但 Marketplace 页面展示尚未发生。100,000 节点本轮 RSS 为 512,540,672 bytes，单次低于 512 MiB；因此前同机运行存在超限波动，性能门仍为 `UNVERIFIED`。

0.1.0 候选的属性推送与脚本合成命令在中英文命令标题和 README 中均标注“实验性/真实编辑器未验收”。在完成无隐私测试元件的读取→差异→二次确认→编辑器画面确认及一次官方脚本合成产物更新前，Marketplace 文案不得暗示这两项已通过；多人证据继续单列，但不是本机扩展 0.1.0 的硬发布门。

## 公开发布身份

- 对外作者和 Publisher 显示名：`不见星光`。
- Marketplace/VS Code 扩展详情标题下的 description 固定为“由不见星光开发，为《元梦之星》创作者提供本地 UI、ID、Lua 与 API 检查能力的 VS Code 伴随工具。”，README 主标题下第一段也以加粗作者开头。
- 计划的 Marketplace Publisher ID 候选：`bujianxingguang`；当前候选扩展 ID：`bujianxingguang.yuanmeng-ai-dev-assistant`。它仅用于本地打包测试，待用户在官方管理页成功创建后才最终确认，未声称已注册或保留。
- `package.json.private=true` 只阻止误发 npm 包，不表示 Marketplace 或 GitHub 私有。
- 源码仓库公开可见，但 `license=UNLICENSED`；不采用 MIT、Apache-2.0 等开源许可证，不授予复制、修改或再分发许可。第三方依赖许可证单独保留。
- 旧开发身份 `yuanmeng-local.yuanmeng-ai-dev-assistant` 不等于 Marketplace Publisher，不能用于公开发布。公开身份切换后必须重跑 Clean Profile 与正常 VSCode 并存验收。

## GitHub 外部状态规则

在所有本机自动化和真实编辑器门槛如实记录前，不创建或推送仓库。当前本机开发历史曾包含不适合公开的旧校准文本，因此不会改写或推送本机开发仓库。发布时只从最终已审计提交的 Git 跟踪文件生成一个位于忽略目录中的干净、单提交公开仓库；该导出不得包含本机 `.git`、`work/`、`outputs/`、真实导出、日志或 VSIX。对导出仓库的工作树和唯一历史重新完成隐私扫描后，才查询 GitHub 目标并操作导出仓库的 `origin`：

- 干净导出没有 `origin` 时，只有在 `gh` 已查询并确认目标为 `PUBLIC` 后才添加其权威 URL；
- 已有 `origin` 且与查询出的公开目标完全一致时才推送；
- 已有 `origin` 不一致、目标不是 `PUBLIC` 或 GitHub 登录/权限失败时立即停止并报告具体阻塞，不盲目 `git remote add`，不伪称已上传。

当前本机开发仓库仍没有 `origin`，没有修改本机开发仓库 remote。公开导出快照单独连接到匹配的 GitHub `origin` 并已推送；本机与公开仓库边界保持隔离。

本机已验证过干净公开导出：提交 `6956001` → 单提交 `0bd5cd8`，文档修订提交 `fab2c3d` → 单提交 `02e5113`，以及本轮最终 URL 元数据提交 `57f015e` → 单提交 `85dc9f6`。本轮最终快照 146 个跟踪文件，工作树/历史隐私扫描和非最终模式 preflight 均零发现；远端 `main` 已核对为 `85dc9f6`，并从 GitHub 新鲜克隆复核通过。后续文档或源代码变化后仍必须重新生成、审计并更新公开快照。

## Marketplace 外部状态规则

Publisher ID 创建后不可更改。当前官方页面尚无已确认 Publisher，本地 VSIX 的 `GalleryFlags=Public` 只表示包的公开展示意图，不证明 Publisher 已存在、包可发布或 Marketplace 已上线。用户只需在真正发布前完成一个账号动作：登录官方 Publisher 管理页并创建或确认计划 ID `bujianxingguang`，显示名为“不见星光”。若 ID 已被占用，发布停止并报告，不能静默换用其他 Publisher。

最终发布只接受通过 `npm run verify`、公开导出历史隐私扫描、`npm run package:release`、VSIX 内容检查和 `public-release-preflight --final-marketplace --publisher-confirmed` 的同一份 VSIX。最终模式还要求真实 `repository`/`homepage`/`bugs`、已确认 `PUBLIC` GitHub 目标、匹配的 origin 和已审计 VSIX；缺少这些字段时预检必须拒绝，不能用 `--allow-missing-repository` 绕过。凭据不得出现在命令参数、仓库、日志或 VSIX；优先采用 Microsoft Entra 身份，使用官方 scoped credential 时只通过受控环境进入发布进程。发布后必须在干净 VSCode Profile 中按名称、中文/英文关键词和完整扩展 ID 搜索、安装、激活、卸载。

此前候选包曾因 GitHub 尚未创建而出现缺 `repository` 警告；本轮真实 URL 已写入 manifest，`npm run package:release` 在同一路径无该警告并通过清单检查。公开仓库与匹配 origin 已完成；最终 Marketplace preflight 仍须等待 Publisher 确认和官方发布。

本轮已创建并推送 GitHub 公开源码快照；没有把本机开发历史、VSIX、真实导出、日志、密钥或官方版权资源提交到仓库。Publisher 创建与 Marketplace 发布仍未执行。
