# Changelog

## 0.6.1

- 精简首页介绍，优先说明六项主要功能、安装准备和 AI 接入方式。
- 新增可复制的本机 AI 提示词、Codex/VS Code Agent 接入步骤与常见问题。
- 将详细命令、场景绑定与实现边界移至独立使用手册，修正 MCP 工具数量为 42。
- 仅更新文档与公开文档导出清单，不改变插件运行功能。

## 0.6.0

- 将原私有版全部功能转为 MIT 开源，保持原 Marketplace 扩展 ID，支持旧公开版本沿用更新渠道。
- 发布完整 VS Code、CLI、MCP 与玩法模拟源码；保留 47 个命令、11 个视图、19 项设置和 42 个完整档 MCP 工具。
- 新增成功启动后的一次性本地开源公告，附完整更新说明；不联网推送、不采集安装者信息。
- 新增不调用 Git 的公开源码导出和 VSIX 打包流程，排除本地历史、地图与账号数据，保留依赖许可全文。
- 要求 VS Code >=1.134；独立 CLI/MCP 要求 Node.js >=20。

## 0.6.0-private.7

- 修正官方审查工具的 MCP 读写标记；默认查询不写入，保存基线属于写操作。
- API 差异补齐函数、常量和枚举的增删改，返回分类明细、完整数量与截断标记。
- 脚本包在配置目录内有界遍历，稳定排序，不跟随符号链接；逐包隔离错误并报告范围、跳过原因和明细截断。
- 标准模板文件增加内存内解压、大小/校验和检查及字节哈希对比，区分缺失、内容不同与无法读取，不覆盖文件。
- VS Code Provider、项目 CLI 与 Codex MCP 启动器共享路径配置；配置改变后串行刷新启动器，保留工程身份和 bundle 校验。
- 自动模拟按事件与运行端去重，优先根事件，为预算内最多 5 个入口分别生成单人、重复、重连与 2/4/8 人场景；其余入口显式记为未覆盖，保留 32 场景上限。
- 本版本静态检查与构建结果不代表安装、官方编辑器或多人实测完成。

## 0.6.0-private.6

- 新增 `official-audit` / `yuanmeng_official_audit`：只读审查官方 API 版本差异、DreamCode 工具箱符号、官方模板 ZIP 结构和 UGC 脚本 ZIP 使用痕迹。
- `api-search` 增加 DreamCode 工具箱事件、值和数据绑定符号检索；不把静态工具箱目录冒充当前地图积木连接图。
- 官方 API 基线只保存到插件私有 `.yuanmeng-inspector/official/`，不覆盖地图工程文件。
- 自动玩法模拟增加事件链入口和 4/8 人同帧竞争矩阵；8 人超过有界时序上限时明确标记截断并要求官方编辑器复测。

## 0.6.0-private.5

- 玩法视图区分当前运行、旧报告和未运行，避免旧 `latest.json` 把新 preview 误显示为整体阻止。
- 工程状态在私有缓存超预算时给出明确提示和安全清理入口。
- `api-search` 接入已安装 DreamCode `api.json`，返回编程元件 API 目录、扩展版本和来源指纹。
- 官方源摘要补充当前扩展贡献的命令清单，便于 AI 判断官方能力是否可用。
- `where-used` 增加有界影响范围摘要，合并 UI/场景/台账候选、Lua 直接引用和运行侧。

## 0.6.0-private.4

- MCP 默认改为日常 `workflow` 工具档，并让项目本地启动器、资源和提示词跟随 profile。
- 增加 MCP 结构化返回 64 KiB 保护、任务上下文短缓存和 API 搜索结果上限。
- 统一官方连接的 Extension Host 与 CLI/MCP 实时状态，降低桥接器空闲写盘频率。
- 普通导入日志不再保存原始行；Lua 诊断增加版本缓存与防抖。

## 0.6.0-private.3

- 优化底部状态栏：工程、地图、连接、刷新和问题拆为独立项，只有地图项可修改本机地图显示名；连接文案统一为“已连接/未连接”，不再把未确认状态写成“待验证”。
- 修复连接状态没有读取官方 Dream Helper 输出的问题：当前 VS Code 窗口会以有界、只读、脱敏方式识别官方 `[地址:端口] 连接成功` 事件；连接证据与 UI 文件刷新证据分开，官方连接成功但 UI 内容未变化时也会显示“已连接”。
- 场景状态栏改为只显示场景元件数，并附带 UI 控件数，不再显示工程文件夹名；玩法视图和完成提示把常见内部状态码转换为中文，机器可读 JSON 状态保持不变。
- 新增只读 `yuanmeng_task_context` 紧凑上下文入口：一次返回工程就绪度、场景/缓存摘要、玩法 latest 和官方扩展/游戏/UGC 数据源索引，输出有界且不暴露绝对路径或完整指纹。
- 新增 `full/workflow/scene/gameplay` 四档 MCP 工具可见性；默认保持 full 兼容，日常 AI 开发可按工程设置缩小工具目录，减少工具选择和上下文负担。
- 新增自动玩法模拟只读预览：CLI `gameplay-test --preview` 与 MCP `preview: true` 完整执行模型但不提交运行包或更新 `latest.json`，用于在不改变地图工程的前提下先验证模拟入口。
- `scene-status` 与工程状态加入只读缓存体积/最大文件摘要；玩法运行包、registry/runtime 和原始 LayerData 不纳入可清理总量，超限只告警不自动删除。
- 自动玩法模拟将场景绑定、对象族和物理证据缺口标为编辑器复核的局部项；手工严格规格和生产入口、可达 Lua、工程身份、模型边界仍 fail-closed。
- 官方数据索引只读取扩展元数据、版本文本、静态配置文件名和 UGC 使用台账；不解密、不执行、不修改地图二进制或账号数据。
- 修复玩法模拟工作流丢失官方 Events 元数据的问题：静态审查包、模拟器和测试报告现在复用同一份已加载的事件声明/文档，不再把合法 Events.* 处理器误报为“没有可用文档”并在模型运行前阻断。
- 补充客户端/服务端目录侧证据与客户端观测标记隔离，避免把客户端观测状态误判为服务端写入；配置 ID 引用不再自动冒充场景事件绑定责任。
- 本版本只修改私有插件源码与候选包元数据，不修改地图工程或地图逻辑。

## 0.6.0-private.2

- 玩法流程模拟默认改为“自动准备并尽可能模拟”：VS Code、CLI 与 MCP 共用同一工作流，从 `GameEntry.lua` 生产可达 Lua、台账、UI/场景和事件元数据生成稳定模型与非空 1/2 人场景；不再要求先手工维护 `spec.json/scenarios`。
- 严格静态门与模拟准入门分离，无关信号盒、不可达坏备份和局部未知 API 不再单独造成 `MODEL=NOT_RUN`；生产入口、可达语法、工程身份、关键 ID 与模型边界仍 fail-closed。
- 三入口统一返回 `model-pass`、`model-fail`、`partial-needs-editor`、`not-run-fatal`，并保留完整严格审查；人工三路径模式作为高级兼容入口，证据过期继续严格阻断。
- 自动运行包原子写入工程私有 `gameplay/runs/<runId>`，只有完整回读和摘要验证后才切换 `latest`；取消、崩溃、跨工程、损坏或缺件不会制造假 current/pass，历史也不会被场景缓存清理。
- 隐私审计与 VSIX 检查明确拒绝玩法运行数据、绝对地图路径、地图 Lua、场景文件和秘密进入候选包。自动模拟不写地图、不自动打包或发布，也不冒充官方编辑器单人或真实多人证据。

## 0.6.0-private.1

- 将条件化任务自检从提示词升级为真实 MCP 工具：`yuanmeng_gameplay_status` 只读返回最新玩法证据，`yuanmeng_task_completion_check` 先做全量静态审查，再按任务规模或连续失败次数决定是否运行已确认玩法模型；从不自动打包或发布。
- `yuanmeng_ids_list`、`yuanmeng_scene_find`、`yuanmeng_scene_types` 与 `yuanmeng_feedback_list` 新增绑定工程、快照、工具和查询条件的签名游标分页；当前 UI、场景、台账和反馈资源默认只取轻量首页，避免大地图一次塞满 AI 上下文。
- `status`/`yuanmeng_project_status` 新增 UI、场景、Lua、API、代码交付和玩法六域的新鲜度、可用性与精确下一步；UI 陈旧不再错误阻断只读场景/Lua 任务。
- 场景运行时分类由 3 类扩展为官方声明中的 6 类：Character、Creature、Element、LogicElement、Player、TriggerBox；补充生物、角色、玩家和逻辑元件的官方事件能力。旧三类证据保持可读，但新增三类维持未知，重新采集后才能安全归类。
- 未知类型 ID 在同类型实例的当前快照运行时对象族证据一致时可自动复用为 `runtime-calibrated`；证据冲突、探针失败或跨快照时仍保持 unknown，不猜官方显示名称。
- 场景审计将常规 candidate/unsupported/absent 字段改为覆盖率汇总，不再为每个实例制造重复警告；真正的重复 ID、快照结构错误、未知 variant 和台账疑似变化仍保留为可执行问题。
- MCP 会话内同一工具、错误码和稳定原因连续失败两次时自动写入一条脱敏本地反馈；不记录原始输入、日志、路径或密钥，反馈失败也不会改变原工具结果。
- 新增 `npm run baseline:private`：把当前插件源码建立为本地私有回滚基线，排除 `.git`、依赖、构建产物、工程私有索引、地图源文件、日志和 `.env`，写入 `outputs/private/rollback/` 的压缩包、SHA-256 清单与版本/Git 元数据；不提交、不上传。

## 0.5.0-private.7

- 新增 MCP 提示词 `yuanmeng_task_completion_check`：AI 交付大型、复杂、多人或高风险玩法改动前，先运行全量静态审查，再对当前已确认规格运行玩法审查和 1/2/4/8 人模型矩阵。
- 同一功能连续出错或返工 2 次及以上时自动升级为玩法自检路线；先按日志/失败证据修正再重跑。该流程不调用自动打包，不替代地图保存、发布或真实多人验收。
- 新增 UI 层级保险：每次官方 UI 更新文件稳定落盘后，按真实 `parentId + siblingIndex` 保存当前工程私有可信基线。
- 仅在同一地图、同一父控件、至少 3 个同级控件、ID 集合完全相同且顺序恰好整体完全反转时报警；普通新增、删除、局部调序和两控件交换不会被冒充为整组反转。
- 检出倒序时冻结上一份可信基线并在 `.yuanmeng-inspector/ui/layer-order-incidents/` 保存结构化证据；插件不会回写、修复或覆盖元梦地图文件。
- 损坏的插件私有层级基线会安全重建，不阻断官方 UI 刷新；手动刷新和文件监听刷新都会给出中文警告与证据路径。

## 0.5.0-private.6

- 代码交付桥对短暂 `EPERM/EBUSY` 原子替换失败进行有界重试；只删除桥目录中符合插件严格命名、已经过期的临时文件，并在租约刚过期时短暂等待恢复。
- 自动交付读取官方 `play.json` 时只保留 `type/pack/time` 并关联本次变化产物，永不输出密码字段或工程外路径。
- `status`/`yuanmeng_project_status` 与插件环境视图新增当前工程 CLI/MCP 启动器、交付桥和官方命令自诊断；扩展仅自动刷新当前工程启动器，不修改全局配置或重载 VS Code。

## 0.5.0-private.5

- 新增精确 `ResolveByNameOrPath`：十进制 ID、完整 UI 层级路径和唯一精确名称可稳定解析；重名、模糊名称和错误路径不会静默选错。
- 新增 `InspectScreenPoint`：按真实设备屏幕坐标返回触摸命中的控件；缺少当前 UI 快照绑定的运行证据时生成白名单只读探针，不猜屏幕坐标或 ZOrder。
- 新增动态 UI 台账：记录 `DuplicateWidget` 返回的控件 ID、`InitListView`/`GetListViewItemUID` 取得的列表运行时子项 ID，并与当前工程和 UI 快照绑定；最多 500 个节点、4 MiB 日志证据。
- 新增通用受控 `RuntimeProbe`，仅允许屏幕点、运行时 UI 树和场景能力三类模板；不接受任意 Lua、旧快照或跨地图日志。
- 新增 `CapabilityDescribe`：综合场景类型、字段与官方只读能力探针，明确区分普通元件碰撞、信号盒进入事件和 unknown；证据不足时不猜事件。
- MCP 工具总数由 33 增至 38；运行日志仅保存脱敏结构化结果，不保留原始日志行。

## 0.5.0-private.4

- `GetTreeScreenSnapshot` 对应的 `yuanmeng_ui_tree_screen_snapshot` 现在严格按 UI 快照中的 `parentId` 递归读取根弹窗、子控件、孙控件及全部更深后代；路径只用于查找与显示，不再用于猜测父子关系。
- 路径相似但不属于该弹窗的控件不会被误收；父子循环、重复实例 ID 和超过 500 个控件的超大树会安全拒绝，不返回不完整布局。

## 0.5.0-private.3

- 新增 `yuanmeng_ui_screen_snapshot`、`yuanmeng_ui_tree_screen_snapshot` 和 `yuanmeng_ui_layout_audit`：用绑定当前工程、当前 UI 快照和精确控件集合的客户端只读探针采集真实屏幕矩形；无运行证据时返回 `EVIDENCE_INSUFFICIENT` 与探针 Lua，不猜坐标。
- UI 运行时日志导入会脱敏保存屏幕/UI 分辨率、矩形、锚点、角度、ZOrder、父级与中心命中信息；布局审计可报告越界、裁切、中心遮挡和可选兄弟控件显著重叠。
- 新增 `yuanmeng_scene_geometry`：只读查询实例/递归编组可信包围盒、承载面贴合状态和严格体积穿插；刚好边界接触不算穿插，缺少可信 bounds 时拒绝推断。
- MCP 工具总数由 29 增至 33；所有新增功能不写地图 Lua、UI 导出或 `LayerData*.dat/pbin`。

## 0.5.0-private.2

- `audit` 和 `yuanmeng_project_audit` 新增安全的定向文件范围与 `errorsOnly` 精简输出，供官方签名已确认的小型 API 修复使用。
- 定向审计固定返回 `scope.mode=targeted`、`fullProjectEvidence=false`、完整问题计数和省略明细计数，防止被误报为全项目通过。
- 默认审计与自动代码交付仍扫描完整 `src`，不自动排除 `*_backup.lua`，没有削弱正式打包门禁。

## 0.5.0-private.1

- 新增私有本机 MCP stdio 服务：29 个工具、7 个资源、3 个提示词；复用现有 CLI/核心，不开放 HTTP 或公网端口。
- VS Code 1.134 使用稳定 `McpServerDefinitionProvider` 自动发现每个当前工程，不依赖公共 MCP Gallery，也不要求 `.vscode/mcp.json`。
- 每工程生成受扩展版本、工程身份、根指纹和 MCP bundle 哈希约束的 `ymai-mcp.cmd`；移动、升级或篡改会刷新或安全拒绝。
- 新增 Codex MCP 注册预览、注册/刷新和卸载命令；只调用正式 Codex CLI，不重写 `config.toml`，并要求新会话实际验证。
- 新增 `yuanmeng_build_and_send_code`：保存当前工程 Lua、通过静态检查、调用官方 `dreamhelper.scriptGen`，只在产物变化或明确官方输出时报告 `BUILT`/`SENT`；不等同地图保存、发布或游戏内共享成功。

## 0.4.0-private.11

- 底部工程状态栏新增当前地图显示名；点击状态栏即可首次填写或随时修改，不再使用容易误解的“未知地图”。
- 新增 `set-map-name <地图名称>` 项目本地 CLI，用户告诉 AI 地图名后，AI 可直接填写；VS Code 自动监听该私有配置并立即刷新状态栏。
- 用户填写的名称只用于本机显示和 AI 识别，不覆盖官方地图身份，也不改变地图指纹、场景绑定、ID 台账或玩法报告证据。
- 未探测的官方联动从“联动:未知”改为“联动:待验证”，明确区分尚未验证与真实离线。

## 0.4.0-private.10

- 新增项目本地结构化反馈箱：用户可从命令面板记录错误、使用阻碍和改进建议，AI 可用 `feedback add/list/resolve --json` 无弹窗收集、汇总和闭环；记录自动绑定当前插件版本、UI 快照和场景快照，不上传且不写地图文件。
- 新增 `scene-types` 官方类型 ID 盘点；按类型聚合实例数、代表 ID、已校准名称/对象族、运行时能力和待校准清单，未知类型保持 unknown，不要求用户逐个口述。
- `scene-audit` 默认聚合重复问题并保留准确总数，`--detailed` 才输出完整明细，减少 AI 上下文和查询耗时。
- UI 内容未变化时保留原快照但推进最近成功检查时间；场景/UI 保存监听、未绑定提示和 AI 下一步说明统一为可执行语义。
- 项目启动器改为轻量 Node 校验器，继续校验工程身份、扩展版本和 CLI 哈希，但不再为每次查询启动 PowerShell/Get-FileHash。
- 结构化日志兼容官方 `[YYYY-M-D HH:mm:ss] [INFO]: [Standalone]` 前缀，并与玩法轨迹共用时间解析，支持按官方日志时间段复盘。

## 0.4.0-private.9

- 修复 AI/项目本地 CLI 在扩展启动后写入场景 binding 时，VS Code 刷新命令仍误报“未接入场景源”的内存不同步问题。
- 场景 binding 文件变化后自动重载侧栏状态；刷新前也会主动核对磁盘 binding，并继续拒绝工程身份不匹配的来源。
- 场景绑定命令支持 AI 直接传入已唯一确认的工程外 UGC `LayerData` 路径；人工文件选择器不再被“Lua 工程内部没有 LayerData”提前拦截。

## 0.4.0-private.8

- 修复无 LayerData 时显式登记实例的 runtime-only 探针生成与日志导入，并明确不冒充完整场景快照。
- 保留重复根信号为显式歧义 registry 记录；补齐场景快照 JSON Schema 的信号、元数据和 UTC 日期约束。
- 场景索引新增根 signalRegistry 多值索引，重复名称不会被覆盖。

## 0.4.0-private.7

- 修正无场景落盘文件时的工作流：状态栏显示“未接入场景源”，绑定命令在找不到官方 LayerData 文件时直接说明场景索引不可用，不再让用户进入空文件选择框；UI、Lua、API 与 AI 静态审查仍可独立使用。
- 无完整场景快照但已有用户显式登记实例时，状态栏改为显示“已登记 N 个元件”，明确区分单元件属性/运行时校准与全场景枚举。
- 场景信号记录、实例索引、编组元数据的未知字段现在保留路径、wire 类型、长度和 SHA-256 摘要；同名根信号不再静默覆盖，而是保留全部记录并标记歧义。
- 玩法静态门接入 Events 声明/文档元数据；服务端直写客户端本地状态、官方事件运行端/回调参数/声明冲突都会阻断。信号触发盒草案同时列出进入与离开事件能力及回调路由确认项。

## 0.4.0-private.6

- 停止向 VS Code “问题”面板发布私有插件静态诊断；官方 API/Lua 检查交由官方扩展和 Lua 工具，玩法静态审查仍由智能体流程执行。
- 保留 UI、场景、注册中心、悬停、代码透镜和显式检索能力。

## 0.4.0-private.5

- 修复官方 API 声明中“默认值/不传时”参数被误报为必填的问题。
- `System:FireSignEvent(EventName)` 和 `UI:SetText(ListItemUID, NewText)` 不再产生 `API_ARGUMENT_COUNT` 静态误报。

## 0.4.0-private.4

- 场景未知 `component6` oneof 现在保留受限的原始 field span/长度/hash 摘要；未知对象族与非布尔对象存在返回保持 `error`，不降级成 `absent`。
- 编组运行时探针不再调用本地官方声明未找到的成员查询 API，改为明确 `unsupported-api` 的静态证据；Events 文档冲突的多个运行端范围会阻断生成资格。
- Lua 索引现在可从事件回调开头的 `if not System:IsServer() ... then return end` 守卫推断该注册的服务端执行语义，无需为常见官方写法额外添加文件级端侧注解。
- “打开最近玩法报告”新增模型、完整模型指纹、审查包和当前工程知识四重校验；旧代码或旧场景的报告会明确标记过期并拒绝打开，避免误读上轮通过结果。

## 0.4.0-private.3

- 修复官方声明带末尾 `...` 时的参数数量误报；`System:RegisterEvent`、`TimerManager:AddFrame` 等两参数合法调用不再被错误阻断。
- AI 玩法草案仍保留完整场景知识图，但只对已识别特殊事件对象、Lua 明确引用实例或与 Lua 信号关联的实例生成玩法确认项，避免把整张地图的未知元件全部变成人工任务。

## 0.4.0-private.2

- 场景适配器升级为 `ym-layerdata-observed-v6`：补充 fixed32 数值自定义属性、信号注册表、图层名、编辑器版本候选、实例索引一致性、编组父子/嵌套/候选元数据和安全未知字段摘要；未知 bytes 仍只保存路径、长度与哈希。
- 信号触发盒类型 `1105000000000087` 接入玩法草案与事件兼容审查；角色进入明确使用 `Events.ON_CHARACTER_ENTER_SIGNAL_BOX`，普通触碰事件仅给普通 Element 使用。
- 玩法事件绑定新增交互意图、外部事件、处理器、引擎权威策略和真实 Lua 注册五重硬门；只在规格里自报“已绑定”不能再让模拟假通过。
- 字段检查器新增场景类型覆盖率，未知类型保持 unknown；按信号名查询可聚合共同 owner 的候选编组，但明确不把编组猜成背包自制物品。
- 场景缓存改为与后台 Worker 共用深层闭合校验，缺字段的实例、编组、信号或元数据快照会被拒绝并要求重新刷新。
- 官方 `.d.lua` 函数、常量、枚举与本地 Events 文档分层索引；API ID 按场景实例、类型、编组、UI、玩家、道具、图片、特效等域分离，避免把资源 ID 当场景 ID。
- 本机通用数据定义加入只读资源搜索，保留文档哈希、分类和冲突，不执行或重新分发官方内容。
- 运行时对象族和字段探针按当前工程/binding/快照/源哈希绑定；位置、旋转、尺寸、可见、物理、碰撞、抓取、父级和子级数量逐项隔离，单项失败不会抹掉其他成功证据。
- 编组操作递归包含全部子编组及 owner 派生成员；贴地预览增加 Element 能力门、执行漂移门和中途失败回滚。
- 新增可撤销的结构化玩法日志探针：在光标处预览插入，记录阶段、运行侧、事件、玩家/实例/位置与有限状态字段；导入时不保存原始日志行。
- 旧适配器快照在扩展启动时自动标记过期并按原绑定安全刷新。所有新增能力仍是私有本机功能，不发布 Marketplace。

## 0.3.0-private.3

- 场景编组字段 2 改按“父编组”解析，适配器升级为 `ym-layerdata-observed-v3`；支持从总编组递归读取子编组和所有成员。
- 贴地计划支持直接输入编组 ID，输入编组成员时保留整体结构，但不会把支撑物所在的同一编组一起移动。
- 修复玩法报告把“2/4/8 人”误当 Unix 绝对路径脱敏的问题。
- `editor-calibration` 测试工程接入编组 626 分端流程：信号盒 517 先贴地，目标玩家客户端移动，服务端确认碰撞后再将货柜 510/511/512 整体贴地。

## 0.3.0-private.2

- 修正真实 `LayerData` 实例记录中元件类型 ID 与 Owner/编组 ID 的字段映射；真实编组成员现在会正确显示长类型 ID和所属编组 ID。
- 场景解析适配器升级为 `ym-layerdata-observed-v2`，防止继续复用旧版本生成的错误场景快照。

## 0.3.0-private.1

- 新增私有“AI 玩法测试助手”：生成需人工确认的玩法知识草案，执行本地严格静态门，再以 1/2/4/8 人矩阵模拟服务端共享、每玩家和客户端本机状态。
- 多人优先覆盖同时操作有界交错、重复投递、广播/单播、共享资源竞争、中途加入/离开/重连、连接代迟到回调、客户端越权和跨玩家污染；任一多人失败覆盖单人通过。
- 报告明确分离 `STATIC_LOCAL`、`MODEL_SIMULATION / UNIT_E2E`、`SCENE_EVIDENCE` 与 `OFFICIAL_EDITOR_MULTI_REQUIRED`；90%–95% 仅指已声明可建模分支覆盖，不代表真实引擎错误检出率。
- 玩法模型绑定当前 Lua、注册表、UI 与场景知识指纹；源码或快照变化后旧模型默认阻断。输出为脱敏 JSON/Markdown 审查包，并只给出不自动修改 Lua 的建议预览。
- 新增安全场景 ID 重绑：同来源快照候选、重复/many-to-one/歧义拒绝、场景+台账+源码三重哈希门，Lua 补丁只预览、需确认且可撤销。
- 新增匿名诊断包和完整场景工作台：字段/空间问题持久视图、层级路径/Lua 常量/JSON/引用/通用布局右键入口、后台 Worker 进度和大场景懒加载。
- UI 与场景自动刷新共用每工程台账事务，修复并发更新时静默丢失 UI 或场景 ID 记录；取消/重载后旧代不能再覆盖 heads、binding 或 registry。

## 0.2.0-private.1

- 转为作者本人本机私有版；Marketplace、公共 GitHub 导出和公共发布预检均无副作用硬拒绝，只允许隐私审计后的私有 VSIX。
- 新增严格 `.dat` ZIP/CRC/data descriptor 容器验证和有界 protobuf wire tokenizer；未知 length-delimited 保持 opaque，fixed 保持 raw bits，uint64 ID 不经 JS number。
- 新增场景实例/类型/owner/变换、编组成员、特殊 oneof 可见化、三来源独立快照、自动文件监听、快照 diff、查询、层级和 ID 台账同步。
- 新增 VS Code 场景树、状态栏、字段证据检查、场景导出、官方 API 测量探针、按自定义属性名定位 ID，以及编组贴地预览/执行 Lua 生成。
- 新增完整场景 CLI：绑定、刷新、状态、查询、树、编组、字段、diff、空间计划、属性定位和 JSON/CSV/Markdown 导出。
- 修复属性读取重复导出相同内容时误超时：内容不变只有在文件签名相对命令前基线确实变化后才接受；旧文件未重写仍会超时。
- 原始 LayerData、私有场景缓存、真实路径、日志和官方二进制加入仓库/VSIX 隐私阻断。

## 0.1.0

- 首个公开发布候选：官方 UI 结构校准、UI 快照/搜索/差异、ID 与信号注册中心、Lua/API 导航、确认式代码补丁、脚本合成观察、元件属性工作流、日志证据分类和本机 CLI。
- 增加工程内 `.yuanmeng-inspector/bin/ymai.cmd` 启动器，不修改系统 PATH。
- 明确静态、自动化、Extension Host、官方编辑器和多人证据等级。
- 对外作者统一为不见星光，扩展详情标题下方的首句直接显示作者名；公开 Marketplace 扩展 ID 为 `bujianxingguang.yuanmeng-ai-dev-assistant`，源码公开可见但未授予开源再许可。
- 增加 256×256 Marketplace PNG 图标、第三方非官方声明、保留权利 LICENSE 和最小化 VSIX 清单；属性推送与脚本合成在真实编辑器验收前明确标为实验性。
- 将 `yaml` 从 2.8.1 定向升级到 2.9.0，并在注册中心导入前限制 4 MiB 大小与 64 层 YAML 嵌套；官方 npm registry 的生产依赖审计为 0 个漏洞。
- 修复交互式元件属性读取误用 UI 刷新 15 秒超时的问题；属性读取改用独立等待配置，错误提示改为属性语境，并将官方空属性表判定为成功。
