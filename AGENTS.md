# 元梦 AI 开发助手维护边界

本仓库是第三方 MIT 开源 VS Code 插件，不是元梦地图工程。维护本仓库时遵守以下短合同：

- 只修改本插件源码、测试、文档和构建产物；不得写入任何地图工程、`.yuanmeng-inspector`、`LayerData*.dat/pbin`、WeGame 地图缓存或地图 Lua。
- 场景源只读；需要真实编辑器、多人或手机证据时明确标为未验证，不把静态索引、模型模拟或 Extension Host 结果冒充实机通过。
- 已有脏工作树必须保留；不执行 `git reset`、`git checkout`、`git clean`、批量覆盖；提交或上传须有当前任务的明确授权。
- 新功能先写失败测试并观察 RED，再做最小实现；依次运行定向测试、类型检查、lint、单元/集成回归和隐私检查。
- MCP 默认保持 `full` 兼容；日常任务可使用 `workflow`、`scene` 或 `gameplay` 工具档。工具档只改变可见目录，不放宽身份、写入或证据门禁。
- 自动玩法模拟只读取生产入口可达 Lua，并把场景/物理/对象族缺口留给官方编辑器；人工规格模式继续严格 fail-closed。
- VSIX 只能先生成和检查；安装、重载、地图刷新、场景绑定、玩法实测和发布必须由用户明确授权。

任务开始优先调用 `yuanmeng_task_context` 获取有界上下文，再按任务需要调用细粒度工具。不要把地图私有 ID、绝对路径、原始日志或二进制内容写入本文件、全局 Skill 或公开产物。
